onEvent('recipes', event => {
  event.shapeless(
  "minecraft:flint_and_steel", // arg 1: output
  [
    'minecraft:iron_ingot',
    'minecraft:flint', 	       // arg 2: the array of inputs
  ]
).stage("three")
})